import 'package:atlas_admin/utils/import.dart';

class CreatePublisher extends StatelessWidget {
  const CreatePublisher({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Publisher'),
      ),
    );
  }
}
